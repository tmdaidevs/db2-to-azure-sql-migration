CREATE FUNCTION SAMPLE.FN_FORMAT_TIMESTAMP (TS TIMESTAMP, FMT VARCHAR(20))
RETURNS VARCHAR(50)
LANGUAGE SQL
RETURN CASE UPPER(FMT)
  WHEN 'YYYYMMDD' THEN VARCHAR_FORMAT(TS, 'YYYYMMDD')
  ELSE 'date format not recognized.'
END

